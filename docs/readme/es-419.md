# Pascoal

> Un IDE moderno de Pascal para escritorio: escribe, compila y ejecuta programas en Pascal.

[English](../../README.md) · [Português (BR)](pt-BR.md) · [Polski](pl.md)

---

## ¿Qué es Pascoal?

Pascoal es un IDE de Pascal para escritorio desarrollado con Tauri y Rust, diseñado para acercar la programación en Pascal a una audiencia moderna. Interfaz limpia, compilación real mediante Free Pascal (FPC), consola interactiva para programas, explorador de archivos y búsqueda entre archivos: todo en un paquete liviano.

Nació como una rebelión contra herramientas anticuadas como Pascalzim, con el objetivo de hacer que Pascal vuelva a ser accesible y divertido, sin el peso de Electron ni el desorden de interfaces heredadas.

## Descarga

Descarga el instalador más reciente para tu plataforma desde la [página de Releases](https://github.com/brener-fregulia/Pascoal/releases/latest):

- **Windows** — `Pascoal_x.x.x_x64-setup.exe`
- **Linux** — `.deb` (Debian/Ubuntu), `.rpm` (Fedora/openSUSE) o `.AppImage` (cualquier distro)

> **Advertencia de SmartScreen en Windows:** el instalador de Pascoal todavía no está firmado digitalmente, así que Windows puede mostrar "Windows protegió tu PC" la primera vez que lo ejecutes. Esto es normal para aplicaciones nuevas sin firmar — haz clic en **Más información** → **Ejecutar de todas formas** para continuar. La advertencia desaparece con el tiempo, a medida que más personas descargan y ejecutan Pascoal.

## Funcionalidades

- **Editor moderno** impulsado por CodeMirror 6, con resaltado de sintaxis estructural para Pascal mediante una gramática Tree-sitter real (no basada en regex) y temas reactivos
- **Compilación real** mediante Free Pascal Compiler (FPC)
- **Consola interactiva** con zonas separadas para la compilación y la salida del programa: `readln` funciona
- **Edición con múltiples pestañas**: abre varios archivos al mismo tiempo
- **Explorador de archivos**: abre una carpeta, navega por sus archivos Pascal, y crea, renombra, elimina, corta/copia/pega archivos y carpetas desde la barra de herramientas, el menú contextual o atajos de teclado
- **Buscar y reemplazar**: widget flotante al estilo VSCode con resaltado de coincidencias, además de búsqueda entre archivos dentro de una carpeta abierta
- **Instalador guiado de FPC**: detecta si Free Pascal no está instalado y ofrece instalarlo automáticamente mediante winget, apt, pacman, dnf o zypper
- **Actualización automática**: comprueba si hay nuevas versiones y las instala automáticamente, con una opción manual para "buscar actualizaciones"
- **Novedades**: una nota corta en la app después de cada actualización, con historial completo y traducido navegable dentro de la app (Ayuda → Historial de Versiones) y un enlace al [changelog](../../CHANGELOG.md) técnico completo
- **Menú nativo del sistema**: menús Archivo, Editar y Ayuda integrados en la barra de título, con un menú Editar completo (deshacer/rehacer, cortar/copiar/pegar, buscar/reemplazar, buscar en archivos, alternar comentario de línea) y enlaces directos para reportar errores o solicitar funciones en GitHub
- **Panel de Configuración** — configura tema, idioma, identidad de git (global o por proyecto), y revisa el estado de instalación de Free Pascal y git, todo en un solo lugar
- **Tres temas**: Oscuro, Claro y Charcoal, con detección del sistema
- **Controles nativos de ventana** adaptados por plataforma (traffic lights en macOS, estilo Windows/Linux)
- **Guardado automático antes de ejecutar**: configurable
- **Interfaz internacionalizada**: English, Português (BR), Español (Latinoamérica) y Polski, con persistencia de la selección de idioma
- **Liviano**: utiliza aproximadamente 170 MB de RAM en Windows (WebView2), 270 MB en Linux (WebKitGTK) y cuenta con instaladores de alrededor de 5 MB

## Requisitos

- [Rust](https://rustup.rs/) (stable)
- [Node.js](https://nodejs.org/) >= 22
- [Free Pascal Compiler (FPC)](https://www.freepascal.org/download.html): no es necesario instalarlo previamente; Pascoal detecta si falta y ofrece instalarlo
- [Requisitos previos de Tauri](https://tauri.app/start/prerequisites/) para tu plataforma

## Primeros pasos

```bash
# Clonar el repositorio
git clone https://github.com/brener-fregulia/Pascoal.git
cd Pascoal

# Instalar dependencias
npm install

# Ejecutar en modo desarrollo
cargo tauri dev

# Generar build de producción
cargo tauri build
```

### Solo frontend (desarrollo de UI sin Tauri)

```bash
npm run dev:ide
```

### Consejos de desarrollo

Para acelerar los rebuilds de Rust durante el desarrollo, instala [sccache](https://github.com/mozilla/sccache):

```bash
cargo install sccache
```

Luego configúralo como wrapper del compilador de Rust en `src-tauri/.cargo/config.toml`:

```toml
[build]
rustc-wrapper = "sccache"
```

Esto también se usa en CI para acelerar los builds en los runners de GitHub Actions.

### Ejecutar las pruebas

```bash
npm test              # frontend + Rust + Pascal
npm run test:frontend # solo Vitest
npm run test:rust     # solo cargo test
npm run test:pascal   # pruebas de integración de Pascal (requiere FPC)
```

## Estructura del proyecto

```text
src/                       # Frontend en Svelte + Vite, organizado por dominio
  app/                       # Shell (Titlebar, ActivityBar, Statusbar), diálogos (About, FpcMissing,
                              # UpdateAvailable, WhatsNew), pantalla de bienvenida
  editor/                    # Integración con CodeMirror, estado de pestañas/sesión, Editor/EditorArea/FindWidget
  language/pascal/           # Lado frontend del resaltado vía Tree-sitter (cliente + decoraciones)
  project/                   # Explorador de archivos y búsqueda entre archivos
  settings/                  # Panel de Configuración (view + páginas de Temas/Idioma/Git/Toolchain) y
                              # su store de persistencia
  toolchain/                 # Orquestación de compilar/ejecutar, consola de build, UI de instalación de FPC
  integrations/
    tauri/                    # Punto único de contacto con el puente IPC de Tauri
    git/                       # Panel y store de Git
    updater/                   # Store de actualización automática
  shared/                    # Componentes entre dominios (IconButton, PanelHeader, Tab, TabBar) y tema
  icons/                     # Componentes de íconos SVG
  i18n/                      # Archivos de locale, store de traducciones, release-notes/ (blurbs de changelog, carga diferida)
  styles/                    # CSS global
src-tauri/
  src/
    lib.rs                    # Solo configuración de la app y registro de comandos
    commands/                 # Adaptadores delgados de los comandos Tauri
    application/              # Casos de uso (analyze_document, run_program, manage_files,
                                # manage_settings, manage_workspace, install_toolchain,
                                # check_toolchain)
    language/pascal/          # Resaltado vía Tree-sitter y su query vendorizada
    project/                  # Explorer, abrir/guardar archivo, búsqueda entre archivos
    toolchain/
      compiler/                 # Compilación con FPC
      installer.rs               # Detección de gestor de paquetes e instalación guiada
      runner.rs                  # Ejecución de procesos (pipes/PTY)
    infrastructure/            # filesystem, git, environment, platform, settings - primitivas del SO
                                # y el store del archivo de settings
    state/                     # Estado compartido de la app (ProcessState)
    tests/                     # Pruebas unitarias de Rust
  tests/
    pascal_runner.rs           # Pruebas de integración Pascal (compila/ejecuta fixtures reales, requiere FPC)
  tauri.conf.json
  Cargo.toml
tests/
  frontend/                  # Pruebas con Vitest
  pascal/                    # Fixtures de pruebas de integración Pascal (.pas + specs.json)
docs/
  readme/                    # Traducciones del README
scripts/
  set-version.cjs            # Script de actualización de versión (también limpia el caché de build de Rust,
                              # avisa si falta la entrada en CHANGELOG.md para la nueva versión)
  extract-changelog.cjs      # Extrae una sección del CHANGELOG.md para el flujo de release
CHANGELOG.md                 # Formato Keep a Changelog, se convierte en el cuerpo de la release en GitHub
```

## Stack tecnológico

| | |
|---|---|
| Runtime | Tauri 2 |
| Backend | Rust |
| Frontend | Svelte 5 + Vite + TypeScript |
| Compilador | Free Pascal (FPC) |
| Editor | CodeMirror 6 |
| Consola | xterm.js |
| Control de versiones | Git (vía CLI) |
| Pruebas | Vitest + cargo test |

## Roadmap

- [x] Archivos recientes (pantalla de bienvenida)
- [x] Abrir carpeta / proyecto
- [x] Buscar y reemplazar, búsqueda entre archivos
- [x] CI/CD con GitHub Actions
- [x] Instalador guiado de FPC (instalación automática mediante winget/apt/pacman/dnf/zypper)
- [x] Comprobador de versiones / actualizador
- [x] Gramática de Pascal con Tree-sitter (resaltado de sintaxis estructural)
- [x] Changelog / notas de versión en la app
- [x] Panel de Configuración (temas, idioma, identidad de git, estado del toolchain)
- [ ] Panel de Git / interfaz de control de versiones (implementada, pero no habilitada aún)
- [ ] Terminal PTY (PowerShell, bash, fish)
- [ ] Ventana de terminal desacoplada para ejecutar programas en Pascal
- [ ] Guías de indentación y esquema del código vía Tree-sitter
- [ ] Modo Playground
- [ ] Modo Challenge con casos de prueba

## Contribuir

Las contribuciones son bienvenidas. Siéntete libre de abrir issues o pull requests. Los reportes de errores y solicitudes de funcionalidades usan [GitHub Issue Forms](../../.github/ISSUE_TEMPLATE/) estructurados, disponibles directamente desde el menú Ayuda de la aplicación.

## Licencia

[MIT](../../LICENSE) - Brener Fregulia, 2026
